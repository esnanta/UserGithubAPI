<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UserGithubAPI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/app_bar_light</item>-->
    </style>

    <style name="Theme.UserGithubAPI" parent="Base.Theme.UserGithubAPI" >
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
             animated drawable. One of these is required. -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/face_animation_avd</item>
        <!-- Required for animated icons -->
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <!--        <item name="android:windowSplashScreenBehavior">icon_preferred</item>-->

        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <item name="postSplashScreenTheme">@style/Theme.UserGithubAPI</item>
    </style>
</resources>
